[project]
name = "godot-mcp-server"
version = "0.1.0"
description = "MCP server for Godot documentation"
readme = "README.md"
requires-python = ">=3.13.5"
dependencies = [
    "mcp[cli]>=1.12.0",
    "pypandoc>=1.15"
]

[tool.uv]
dev-dependencies = []